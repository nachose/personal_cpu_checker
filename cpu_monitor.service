[Unit]
Description=CPU Monitor Daemon
After=network.target

[Service]
Type=simple
ExecStart=/path/to/your/cpu_monitor.sh  # Replace with the actual path to your script
Restart=always

[Install]
WantedBy=multi-user.target

